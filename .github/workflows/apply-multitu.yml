name: Stage incremental multi-translation-unit refresh

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  waiting:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Patch parts are being staged."
